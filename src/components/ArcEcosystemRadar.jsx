// Moved to ./ui/ArcEcosystemRadar.jsx (shadcn-style components live in components/ui).
// Shim kept so any old import path still resolves. Safe to delete once unused.
export { default } from "./ui/ArcEcosystemRadar";
